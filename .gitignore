# Build artifacts — never commit (images are multi-GB)
/output/
*.img
*.img.zst
*.img.gz
*.raucb

# Secrets / keys — never commit
/certs/
*.pem
*.key
*.crt
!server/**/*.conf

# Local server config
server/.env

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/
